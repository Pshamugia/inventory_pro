<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route local Apache requests to Laravel's public front controller when the
    # virtual host points at the project root instead of the public directory.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
